apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: hello-api
  namespace: flux-system
spec:
  image: whlb/pyproject-gitops-demo
  interval: 1m0s        # poll DockerHub every minute
